extends layout

block styles
	link(rel='stylesheet', href='/css/index.css')

block scripts
	script.
		$(document).ready(function() {
			var curr = 0;
			$('#portfolio').hide();

			$(window).mousemove(function(e) {
				var index = Math.floor(e.pageX / $(window).width() * 4);

				if(index != curr) {
					$('#pos').html(e.pageX + ' (' + index + ')');
					$('#background').removeClass();
					$('#background').addClass('background-' + index);
					curr = index;

					if(index == 3) {
						$('#portfolio').fadeIn(500);
					} else {
						$('#portfolio').hide();
					}

				}
			});
		});

block content
	div#background.background-0
		p#about
			| #{greeting}, I'm Herald Gutierrez. I graduated from the University of Manitoba with a Bachelors of Science: Computer Science degree. I am currently working towards being a better web developer. Samples of my work can be found on my  
			a(href='/portfolio') portfolio
			|  and my work history can be found either on my 
			a(href='/resume') resume
			|  or my 
			a(href='http://ca.linkedin.com/pub/herald-gutierrez/34/337/9b0') LinkedIn profile.
			|  If you'd like to find out more about myself, 
			a(href='mailto:herald@heraldgutierrez.com') say hello,
			|  I'd love to hear from you.
			br
			span - Herald Gutierrez
		a(href='/portfolio') 
			div#portfolio
				center
					div Sample Work